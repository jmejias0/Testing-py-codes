first_number = input("First: ")  # Receiving input from the user and storing it in the variable 'first_number'
second_number = input("Second: ")  # Receiving input from the user and storing it in the variable 'second_number'

print(float(first_number) + float(second_number))  # Print the sum of the two numbers to the console

course = "lets goign to practice Python"
print(course.upper())